Creating a linear model to predict the stock price for reliance stock (Prediction of Open Price).

Libraries we are using
import pandas as pd  (for DataFrame and related operation)
import matplotlib.pyplot as plt  (for visualization)
import seaborn as sns  (for visualization)
import plotly.express as px  (for visualization)
 
from sklearn.linear_model import LinearRegression  (for creating linear model)
from sklearn.metrics import mean_absolute_error  (for evaluation of model)